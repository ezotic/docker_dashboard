# Environment variables for Docker Dashboard

# Flask configuration
FLASK_ENV=production
FLASK_DEBUG=False

# Optional: Bind to a specific IP
# Default: 0.0.0.0 (listens on all interfaces)
FLASK_HOST=0.0.0.0

# Optional: Port to run on
# Default: 5000
FLASK_PORT=5000
